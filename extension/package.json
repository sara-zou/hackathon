{
  "name": "livecheck-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "vite build && cp public/manifest.json dist/",
    "dev": "vite build --watch"
  },
  "dependencies": {
    "preact": "^10.25.0",
    "@preact/signals": "^1.3.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "typescript": "^5",
    "vite": "^6.3.0"
  }
}
